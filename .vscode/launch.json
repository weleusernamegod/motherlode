{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "emulicious-debugger",
            "request": "launch",
            "name": "Launch in Emulicious",
            "program": "${workspaceFolder}/Motherlode.gbc",
            "port": 58870,
            "stopOnEntry": false,
            "preLaunchTask": "${defaultBuildTask}",
        }
    ]
}